import React, { FC, Fragment } from 'react';
import Filters from './Filters';

const App: FC = () => {
    return (
        <Fragment>
            <Filters />
        </Fragment>
    );
};

export default App;
